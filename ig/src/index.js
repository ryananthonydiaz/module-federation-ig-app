/**
 * Must bootstrap application. If you don't do this your application
 * will fail to boot and complain in the console about not being able
 * to eagerly load a dependency.
 */
import("./realIndex");
